"use client";

import React from "react";

const AddToCart = () => {
  return (
    <div>
      <button className="btn btn-accent btn-outline mt-3" onClick={() => console.log("Clicked")}>Add to Card</button>
    </div>
  );
};

export default AddToCart;
